<section class="partners" data-reveal>
  <div class="partners__logos">
    <div class="partner-card glass-panel">
      <svg viewBox="0 0 120 32" class="partner-logo">
        <rect width="120" height="32" rx="6" fill="currentColor" opacity="0.12"/>
        <circle cx="20" cy="16" r="6" fill="currentColor" opacity="0.3"/>
        <text x="34" y="21" font-family="Inter,sans-serif" font-size="11" font-weight="600" fill="currentColor" opacity="0.5">BANCO PROV</text>
      </svg>
    </div>
    <div class="partner-card glass-panel">
      <svg viewBox="0 0 120 32" class="partner-logo">
        <rect width="120" height="32" rx="6" fill="currentColor" opacity="0.12"/>
        <circle cx="20" cy="16" r="6" fill="currentColor" opacity="0.3"/>
        <text x="34" y="21" font-family="Inter,sans-serif" font-size="11" font-weight="600" fill="currentColor" opacity="0.5">FINANCIERA</text>
      </svg>
    </div>
    <div class="partner-card glass-panel">
      <svg viewBox="0 0 120 32" class="partner-logo">
        <rect width="120" height="32" rx="6" fill="currentColor" opacity="0.12"/>
        <rect x="14" y="10" width="12" height="12" rx="3" fill="currentColor" opacity="0.3"/>
        <text x="34" y="21" font-family="Inter,sans-serif" font-size="11" font-weight="600" fill="currentColor" opacity="0.5">CRED pago</text>
      </svg>
    </div>
    <div class="partner-card glass-panel">
      <svg viewBox="0 0 120 32" class="partner-logo">
        <rect width="120" height="32" rx="6" fill="currentColor" opacity="0.12"/>
        <path d="M16 8l6 6-6 6" stroke="currentColor" stroke-width="2" fill="none" opacity="0.3"/>
        <text x="34" y="21" font-family="Inter,sans-serif" font-size="11" font-weight="600" fill="currentColor" opacity="0.5">PAGO FACIL</text>
      </svg>
    </div>
  </div>
</section>

<style is:global>
  .partners {
    padding-block: 60px;
    text-align: center;
    position: relative;
    z-index: 2;
  }

  .partners__logos {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 24px;
    flex-wrap: wrap;
    max-width: 1000px;
    margin: 0 auto;
    padding: 0 20px;
  }

  .partner-card {
    padding: 20px 32px;
    border-radius: 16px;
    background: linear-gradient(135deg, rgba(255,255,255,0.02), rgba(255,255,255,0.0));
    transition: transform 300ms ease, box-shadow 300ms ease;
  }

  .partner-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 10px 30px rgba(0,0,0,0.5);
  }

  .partner-logo {
    height: 32px;
    width: 120px;
    opacity: 0.7;
    transition: opacity 200ms ease;
    flex-shrink: 0;
  }

  .partner-card:hover .partner-logo { opacity: 1; }
</style>
