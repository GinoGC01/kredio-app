<section class="hero">
  <div class="blob-scene" aria-hidden="true">
    <div class="blob blob--primary"></div>
    <div class="blob blob--secondary"></div>
    <div class="blob blob--accent"></div>
  </div>

  <div class="hero__content">  

    <h1 class="hero__headline">
      <span class="line"><span>controlá</span></span>
      <span class="line indent"><span>tus<span class="hero__headline--accent">crédito</span></span></span>
    </h1>

    <div class="hero__cta-container">
      <div class="hero__cta glass-panel">
        <span class="cta-text">verificá que sos humano</span>
        <button class="btn btn--primary cta-btn">Probá gratis</button>
      </div>
    </div>

    <p class="hero__sub--bottom">
      Protegiendo tu información con tecnología de punta,<br>empoderándote con privacidad en todas partes.
    </p>
  </div>
</section>

<style is:global>
  .hero {
    min-height: 100svh;
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
    overflow: hidden;
    padding: 120px 5% 80px;
    text-align: center;
  }

  .hero__content {
    position: relative;
    z-index: 1;
    width: 100%;
    max-width: 1200px;
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  .hero__headline {
    font-family: 'Syne', sans-serif;
    font-size: var(--text-display);
    font-weight: 800;
    line-height: 0.9;
    letter-spacing: -0.03em;
    color: var(--c-text-primary);
    margin: 60px 0 80px;
    text-shadow: 0 0 40px rgba(255,255,255,0.1);
  }

  .hero__headline .line {
    display: block;
    overflow: hidden;
  }
  
  .hero__headline .line.indent {
    margin-left: 15%;
  }

  .hero__headline .line span {
    display: block;
    transform: translateY(110%);
    animation: line-enter 800ms cubic-bezier(0.16, 1, 0.3, 1) forwards;
  }

  .hero__headline .line:nth-child(1) span { animation-delay: 200ms; }
  .hero__headline .line:nth-child(2) span { animation-delay: 350ms; }

  @keyframes line-enter {
    to { transform: translateY(0); }
  }

  .hero__headline--accent {
    background: linear-gradient(135deg, #ffffff, #a0a0a0);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .hero__sub--left {
    position: absolute;
    left: 0;
    top: 20%;
    text-align: left;
    font-size: 13px;
    color: var(--c-text-secondary);
    line-height: 1.6;
    max-width: 220px;
    opacity: 0;
    animation: fade-in 1s ease 800ms forwards;
  }

  .hero__sub--bottom {
    font-size: 14px;
    color: var(--c-text-secondary);
    line-height: 1.6;
    max-width: 500px;
    margin-top: 60px;
    opacity: 0;
    animation: fade-in 1s ease 1000ms forwards;
  }

  /* Floating Stats */
  .floating-stat {
    position: absolute;
    display: flex;
    flex-direction: column;
    opacity: 0;
    animation: fade-in 1s ease 600ms forwards;
  }

  .floating-stat .stat__number {
    font-family: 'Syne', sans-serif;
    font-size: 24px;
    font-weight: 700;
    color: var(--c-text-primary);
  }

  .floating-stat .stat__label {
    font-size: 11px;
    color: var(--c-text-muted);
    text-transform: lowercase;
  }

  .stat__line {
    position: absolute;
    background: rgba(255,255,255,0.2);
  }

  .stat-1 {
    top: 10%;
    right: 15%;
    text-align: right;
  }
  .stat-1 .stat__line {
    width: 1px;
    height: 80px;
    bottom: -90px;
    right: 20px;
    transform: rotate(-30deg);
  }

  .stat-2 {
    top: 45%;
    left: 5%;
    text-align: left;
  }
  .stat-2 .stat__line {
    width: 60px;
    height: 1px;
    top: 12px;
    right: -80px;
    transform: rotate(15deg);
  }

  .stat-3 {
    top: 35%;
    right: 5%;
    text-align: right;
  }
  .stat-3 .stat__line {
    width: 1px;
    height: 100px;
    top: -110px;
    left: 20px;
    transform: rotate(20deg);
  }

  /* Glassmorphic CTA */
  .hero__cta-container {
    opacity: 0;
    animation: fade-up 700ms cubic-bezier(0.16, 1, 0.3, 1) 900ms forwards;
  }

  .hero__cta {
    display: inline-flex;
    align-items: center;
    gap: 24px;
    padding: 8px 8px 8px 24px;
    border-radius: 40px;
    background: rgba(255, 255, 255, 0.05);
  }

  .cta-text {
    font-size: 13px;
    color: var(--c-text-secondary);
    letter-spacing: 0.02em;
  }

  .cta-btn {
    border-radius: 30px;
    padding: 12px 32px;
    font-size: 14px;
    background: linear-gradient(135deg, var(--c-accent), #ff5e00);
    box-shadow: 0 4px 20px rgba(255, 94, 0, 0.3);
  }

  @keyframes fade-up {
    from { opacity: 0; transform: translateY(20px); }
    to   { opacity: 1; transform: translateY(0); }
  }

  @keyframes fade-in {
    to { opacity: 1; }
  }

  @media (max-width: 1023px) {
    .hero__headline { font-size: clamp(52px, 14vw, 100px); }
    .hero__sub--left { position: static; text-align: center; max-width: 100%; margin-bottom: 20px; }
    .floating-stat { display: none; /* Hide on small screens to avoid clutter */ }
    .hero__headline .line.indent { margin-left: 0; }
  }
</style>
