<nav class="navbar">
  <a class="navbar__logo" href="/">
    <svg width="100" height="28" viewBox="0 0 100 28" fill="none">
      <text x="0" y="22" font-family="'Syne', sans-serif" font-size="22" font-weight="800">kredio</text>
    </svg>
  </a>
  <button class="navbar__hamburger" aria-label="Menu">
    <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
      <path d="M3 12h18M3 6h18M3 18h18"/>
    </svg>
  </button>
  <ul class="navbar__links">
    <li><a href="#features">Funciones</a></li>
    <li><a href="#stats">Estadísticas</a></li>
    <li><a href="#cta">Contacto</a></li>
  </ul>
  <div class="navbar__actions">
    <a class="btn btn--ghost" href="/login">Ingresar</a>
    <a class="btn btn--primary" href="/signup">Empezar gratis</a>
  </div>
</nav>

<style is:global>
  .navbar {
    position: fixed;
    top: 0; left: 0; right: 0;
    z-index: 100;
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 20px 5%;
    transition: background 300ms ease, backdrop-filter 300ms ease;
  }

  .navbar.scrolled {
    background: rgba(8, 8, 8, 0.75);
    backdrop-filter: blur(12px);
    border-bottom: 1px solid var(--c-border);
  }

  .navbar__logo {
    text-decoration: none;
    color: var(--c-text-primary);
    font-family: 'Syne', sans-serif;
    font-size: 22px;
    font-weight: 800;
  }

  .navbar__logo svg text {
    fill: var(--c-text-primary);
  }

  .navbar__links {
    display: flex;
    gap: 32px;
    list-style: none;
  }

  .navbar__links a {
    color: var(--c-text-secondary);
    font-size: 14px;
    font-weight: 500;
    text-decoration: none;
    transition: color 150ms ease;
  }

  .navbar__links a:hover { color: var(--c-text-primary); }

  .navbar__hamburger {
    display: none;
    background: none;
    border: none;
    color: var(--c-text-primary);
    cursor: pointer;
  }

  .navbar__actions {
    display: flex;
    gap: 8px;
    align-items: center;
  }

  @media (max-width: 767px) {
    .navbar__links { display: none; }
    .navbar__hamburger { display: flex; }
    .navbar__actions .btn--ghost { display: none; }
  }
</style>
